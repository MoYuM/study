---
题目: "并发限制调度器？"
分类: 代码题
频率: 高频
id: e4c38e48-78b5-4c15-b0ba-1e2991eb3c83
---
```tsx

/**
  JS实现一个带并发限制的异步调度器Scheduler，保证同时运行的任务最多有两个。
  完善代码中的Scheduler类，使得以下程序能正确输出。
  考到2次
 */

class Scheduler {
	constructor(count) {
		this.count = 2
		// 为什么需要两个队列？
		// 因为这里会用到递归，在递归的时候需要拿到其他 promise
		// 所以要存一下
		this.queue = []  
		this.run = []
	}

	add(task) {
		this.queue.push(task)
		return this.schedule()
	}

	schedule() {
		if (this.run.length < this.count && this.queue.length) {
				// 队列中取出一个 promise
		  	const task = this.queue.shift()
		  	// 设置回调
		  	const promise = task().then(() => {
			  	// 这里将 promise 删除
		  		this.run.splice(this.run.indexOf(promise), 1)
		  	})
		  	// 推入执行队列
		  	this.run.push(promise)
		  	return promise
		} else {
		  	return Promise.race(this.run).then(() => this.schedule()) // 这里递归
		}
	}
}

const timeout = (time) => new Promise(resolve => {
  setTimeout(resolve, time);
});

const scheduler = new Scheduler();

const addTask = (time, order) => {
  scheduler
    .add(() => timeout(time))
    .then(() => console.log(order));
}

addTask(1000, '1')
addTask(500, '2')
addTask(300, '3')
addTask(400, '4')

// output: 2 3 1 4
// 一开始，1、2两个任务进入队列
// 500ms时，2完成，输出2，任务3进队 
// 800ms时，3完成，输出3，任务4进队 
// 1000ms时，1完成，输出1 
// 1200ms时，4完成，输出4
```
