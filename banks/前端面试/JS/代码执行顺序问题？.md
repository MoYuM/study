---
题目: "代码执行顺序问题？"
分类: JS
频率: 高频
id: d23b7ecc-89b6-4e49-9a86-4559697f7c6e
---
考点是事件循环：同步代码 → 当前宏任务结束后清空所有微任务 → 取下一个宏任务。

分析口诀：先走同步；遇到 `Promise.then`/`await` 之后的代码进微任务队列；`setTimeout`/`setInterval` 进宏任务队列；每个宏任务执行完都会把微任务清空。`async` 函数中 `await` 右侧表达式同步执行，其后的代码相当于 `.then` 回调。
