---
题目: "数组逆序"
分类: 算法
频率: 低频
id: 0456b233-1692-47ec-8281-bbfd04daae26
---
- `arr.reverse()`：原地反转，会修改原数组。
- 不改原数组：`[...arr].reverse()` 或 `arr.slice().reverse()`。
- 双指针：首尾交换，i 从 0、j 从末尾，交换并向中间收拢，时间 O(n)、空间 O(1)。
- reduce：`arr.reduce((acc, cur) => [cur, ...acc], [])`（性能较差）。
