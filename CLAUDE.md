
本项目为基于 /mastery-drill skill 的学习系统，根据用户需要，自动调用 skill 触发。

# 规则

- **禁止新建 branch 或 worktree**，所有操作直接在 master 上进行。

# 题库

- 题库目录为 `banks/<题库>/<分类>/<题目>.md`
- 一个题目包含如下部分
	- 题干
	- 元数据
	- 答案：按照 **金字塔原理** 结构化，分层编写，方便记忆
	- 追问：由题目引申和发散出的，更深层的原理和细节等
	- 参考资料：权威资料的引用

## 记忆方法：发展史链条（用于易混淆 / 有演化史的知识点）

陪练中发现有些错误不是"忘了"（衰减），而是"记混了"（干扰）——把两个长得像的概念张冠李戴，越复习越强化错误关联，靠"多背几次"治不好。这类知识点，以及本身有真实设计演化史的技术概念（语言特性、协议、架构等），答案部分改写成"问题→方案"链条，而不是直接给结论：

- 每一步用 `❓` 标一句话问题、`✅` 标一句话方案，不用数字序号
- 链条必须基于**真实、可查证的历史**（论文、规范版本号、时间线），绝不虚构"假设你要设计一门语言……"这类编造的动机
- 步骤之间逻辑要衔接：后一步的"问题"必须是前一步"方案"直接引出的新问题，不能跳步
- 最后一步是"现状"，把易混淆的两个点摆在一起对比收尾
- 单独成一节（`## 发展史（问题 → 方案的链条）`），不要和追问/苏格拉底式提问揉在一起
- 不是万能的：纯算法/数据结构题（如「有效括号」「列表转树」）没有设计演化史可讲，不要硬套，用别的方式加强记忆
- 已用此方法改写的例子：`banks/前端面试/JS/原型链是什么？顶层是什么？.md`、`banks/前端面试/AI/Transformer 原理简述？.md`、`banks/前端面试/AI/什么是 Agent Loop ／ ReAct？.md`

# 进度跟踪

每个题库都有一个对应的进度跟踪文件，基于 obsidian 的 base 能力。

- 进度存放在 `progress/<题库>/<题目Id>.md` 和 `progress/<题库>.base`
- 题库和进度跟踪分离，保持题库的干净
